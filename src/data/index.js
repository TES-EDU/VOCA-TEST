import level3 from './books/level3.json';
import sample from './books/sample.json';

const vocabularyData = {
    textbooks: [
        sample,
        level3
    ]
};

export default vocabularyData;
